Task 10: Ghost rendering in all 3 viewports
============================================

Date: 2026-03-25

## Changes Made

### visual_cues.py
- Added `from exca_dance.rendering.theme import NeonTheme` import
- Removed hardcoded `GHOST_ALPHA = 0.30` constant
- Ghost model now uses NeonTheme.GHOST_* colors via ghost_colors dict:
  - base/turret: GHOST_SWING (violet)
  - BOOM: GHOST_BOOM (purple)
  - ARM: GHOST_ARM (light purple)
  - BUCKET: GHOST_BUCKET (pink-purple)
- render_ghost() uses NeonTheme.GHOST_ALPHA (0.55) instead of old 0.30

### gameplay_screen.py
- Added `import moderngl`
- Ghost now renders in all 3 viewports (main_3d, top_2d, side_2d)
- Each viewport is activated via ViewportManager.set_viewport() before ghost render
- Full viewport reset after ghost rendering, before HUD overlay

## Verification

### pytest
$ PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 .venv/bin/pytest --tb=short -q
56 passed in 0.14s

### Ghost colors check
$ python -c "from exca_dance.rendering.visual_cues import VisualCueRenderer; import inspect; src=inspect.getsource(VisualCueRenderer.__init__); assert 'GHOST_BOOM' in src or 'ghost_colors' in src; print('Ghost colors used. PASS')"
Ghost colors used. PASS

## RESULT: PASS
